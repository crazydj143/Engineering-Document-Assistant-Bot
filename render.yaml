services:
  - type: worker
    name: engineering-document-assistant-bot
    runtime: python
    buildCommand: pip install -r requirements-render.txt
    startCommand: python chatbot/telegram_bot.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: GROQ_API_KEY
        sync: false
